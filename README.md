# 🧠 AI-KYC — Интеллектуальная система проверки личности (KYC) на базе локальных нейросетей

## 📋 Описание

**AI-KYC** — это модуль для автоматической обработки паспортных данных с помощью локальных нейронных сетей.  
Он сочетает в себе **распознавание текста (OCR)**, **постобработку с помощью LLM (Mistral)** и **интеграцию с банковскими микросервисами**.

Проект спроектирован как часть архитектуры **banking-mvp**, но может быть использован и как независимый сервис для любых систем верификации личности.

---

## ⚙️ Возможности

✅ **Локальный OCR (EasyOCR)**  
Распознаёт текст с паспортов РФ без отправки данных во внешние сервисы.  
Работает на **GPU (CUDA)**, поддерживает **русский и английский языки**.

✅ **Облачный OCR.Space API (резерв)**  
Используется при необходимости уточнения результата или при отсутствии GPU.

✅ **Интеграция с Mistral через Ollama**  
После OCR текст передаётся в локальную модель `mistral`,  
которая формирует структурированный JSON:
```json
{
  "ФИО": "Иванов Иван Иванович",
  "Серия": "1234",
  "Номер": "567890",
  "ДатаВыдачи": "01.01.2015",
  "КодПодразделения": "290-000"
}
```

✅ **Полностью локальная архитектура**  
Все этапы — от OCR до генерации JSON — выполняются локально.  
Никаких внешних API, кроме OCR.Space (опционально).

---

## 🧩 Архитектура

```
📂 ai-kyc/
 ├── pom.xml
 ├── src/main/java/com/mvp/kyc/
 │   ├── controller/
 │   │   └── AiKycOcrController.java      ← REST API
 │   ├── service/
 │   │   └── AiKycOcrService.java         ← OCR + LLM логика
 │   └── model/
 │       └── KycResult.java               ← Модель результата
 └── python/
     └── local_ocr_server.py              ← Flask + EasyOCR сервер
```

---

## 🚀 Установка и запуск

### 1. Установка Python OCR сервиса

```bash
cd modules/ai-kyc/python
pip install flask easyocr torch torchvision pillow
python local_ocr_server.py
```

Сервис запустится на `http://localhost:5000/ocr`

---

### 2. Подключение Ollama (Mistral)

```bash
ollama pull mistral
ollama serve
```

Проверка:
```bash
curl http://localhost:11434/api/tags
```

---

### 3. Сборка и запуск модуля

Если проект в Docker:
```bash
docker compose build
docker compose up
```

---

## 🧠 REST API

### 🔹 Проверка паспорта

`POST /api/kyc/ocr/passport`

**Параметры:**
| Имя | Тип | Описание |
|-----|-----|-----------|
| file | multipart/form-data | Изображение паспорта |

**Ответ:**
```json
{
  "ok": true,
  "details": "{ \"ФИО\": \"Иванов Иван Иванович\", \"Серия\": \"1234\", \"Номер\": \"567890\" }"
}
```

---

## 🧰 Технологический стек

- **Java 17 / Spring Boot 3.3**
- **Python 3.11 / Flask / EasyOCR**
- **Ollama + Mistral LLM**
- **OkHttp + Jackson**
- **Docker Compose**

---

## 💡 Идея проекта

Цель — показать, как можно реализовать **полностью офлайн KYC-систему**,  
использующую **нейронные сети** и **LLM для структурирования данных**,  
без зависимости от внешних сервисов и рисков утечки персональных данных.

---

## 🧾 Лицензия

Проект распространяется под лицензией **MIT**.  
Вы можете свободно использовать, модифицировать и интегрировать его в свои решения.
